services:
  aspdotnet-project:
    image: aspdotnet-project
    build:
      context: .
      dockerfile: aspdotnet-project/Dockerfile
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
    ports:
      - "5295:8080"
    env_file:
      - ./aspdotnet-project/.env